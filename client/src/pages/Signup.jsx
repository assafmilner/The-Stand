import React from "react";
import SignupForm from "../components/auth/SignupForm";
import "../styles/auth.css";

/**
 * Signup Page
 *
 * A simple page that renders the SignupForm component.
 * Assumes layout and styling are handled inside the form or globally.
 */

const Signup = () => {
  return <SignupForm />;
};

export default Signup;
