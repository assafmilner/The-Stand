import VerifyEmail from "../components/auth/VerifyEmail";

/**
 * Verify Email Page
 *
 * Displays the email verification process (e.g. token validation, confirmation).
 * Assumes layout and UI handled in <VerifyEmail /> component.
 */

const VerifyEmailPage = () => {
  return <VerifyEmail />;
};

export default VerifyEmailPage;
